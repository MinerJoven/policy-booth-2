# 德区政策展台 — 开发规格文档 (Technical Specification)

> 版本：v1.0 | 基于 PRD v1.0 | 适用范围：第一阶段开发

---

## 目录

1. [技术栈选型](#1-技术栈选型)
2. [项目结构](#2-项目结构)
3. [数据库设计](#3-数据库设计)
4. [路由与页面规划](#4-路由与页面规划)
5. [核心组件规划](#5-核心组件规划)
6. [API 设计](#6-api-设计)
7. [搜索实现](#7-搜索实现)
8. [内容状态机](#8-内容状态机)
9. [后台管理](#9-后台管理)
10. [非功能需求实现](#10-非功能需求实现)
11. [部署方案](#11-部署方案)
12. [开发优先级与 Milestone](#12-开发优先级与-milestone)
13. [待确认问题](#13-待确认问题)

---

## 1. 技术栈选型

### 1.1 推荐栈

| 层级 | 技术 | 理由 |
|---|---|---|
| 前端框架 | **Next.js 14 (App Router)** | SSR/SSG 同时支持，SEO 友好，适合内容型网站 |
| 语言 | **TypeScript** | 类型安全，字段众多，避免运行时错误 |
| 样式 | **Tailwind CSS** | 响应式快，中文内容排版便利 |
| 数据库 | **Supabase (PostgreSQL)** | 托管 PostgreSQL + 内置 Auth + REST API，免费 tier 可用 |
| 全文搜索 | **PostgreSQL FTS + Supabase** | 第一阶段内置即可，中文需配置（见第 7 节） |
| 后台管理 | **自建 Admin 页面（/admin）** | 轻量，完全受控，避免第三方 CMS 依赖 |
| 认证 | **Supabase Auth** | 运营人员后台登录用，无需用户注册 |
| 部署 | **Vercel** | Next.js 原生支持，自动 Preview 环境 |
| 富文本编辑 | **Tiptap (React)** | 适合中文政策正文编辑，支持 Markdown 导入 |

### 1.2 不引入的内容（第一阶段）

- 用户注册 / 个人账号系统（PRD 未要求）
- 订阅 / 推送通知（列为后续功能）
- 外部 CMS（如 Contentful、Sanity）— 维护成本过高

---

## 2. 项目结构

```
德区政策展台/
├── app/                        # Next.js App Router 页面目录
│   ├── (public)/               # 公开页面路由组
│   │   ├── page.tsx            # 首页 /
│   │   ├── policies/
│   │   │   ├── page.tsx        # 政策列表页 /policies
│   │   │   └── [slug]/
│   │   │       └── page.tsx    # 政策详情页 /policies/[slug]
│   │   ├── categories/
│   │   │   └── [category]/
│   │   │       └── page.tsx    # 分类页 /categories/[category]
│   │   ├── regions/
│   │   │   └── [region]/
│   │   │       └── page.tsx    # 地区页 /regions/[region]
│   │   ├── search/
│   │   │   └── page.tsx        # 搜索结果页 /search
│   │   └── about/
│   │       └── page.tsx        # 关于我们 /about
│   ├── admin/                  # 后台管理路由（需登录）
│   │   ├── layout.tsx          # Admin 布局（验证 Session）
│   │   ├── page.tsx            # Admin 首页 /admin
│   │   ├── policies/
│   │   │   ├── page.tsx        # 政策列表管理
│   │   │   ├── new/
│   │   │   │   └── page.tsx    # 新增政策
│   │   │   └── [id]/
│   │   │       └── page.tsx    # 编辑政策
│   │   └── login/
│   │       └── page.tsx        # 后台登录页
│   └── api/                    # API Routes
│       ├── policies/
│       │   ├── route.ts        # GET 列表 / POST 新建
│       │   └── [id]/
│       │       └── route.ts    # GET 详情 / PATCH 更新 / DELETE 删除
│       ├── search/
│       │   └── route.ts        # GET 搜索
│       └── admin/
│           └── auth/
│               └── route.ts    # 登录 / 登出
├── components/                 # 可复用组件
│   ├── policy/
│   │   ├── PolicyCard.tsx      # 政策卡片
│   │   ├── PolicyDetail.tsx    # 政策详情主体
│   │   ├── RiskBadge.tsx       # 风险等级徽章
│   │   ├── ReviewBadge.tsx     # 复核状态徽章
│   │   ├── TargetGroupTags.tsx # 适用人群标签
│   │   └── RelatedPolicies.tsx # 相关政策推荐
│   ├── filter/
│   │   ├── FilterPanel.tsx     # 筛选面板
│   │   ├── RegionFilter.tsx    # 地区筛选
│   │   ├── CategoryFilter.tsx  # 分类筛选
│   │   ├── AudienceFilter.tsx  # 人群筛选
│   │   └── DateFilter.tsx      # 时间筛选
│   ├── search/
│   │   └── SearchBar.tsx       # 搜索框
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   └── DisclaimerBanner.tsx
│   └── admin/
│       ├── PolicyForm.tsx      # 政策新增 / 编辑表单
│       └── StatusControl.tsx   # 状态管理控件
├── lib/
│   ├── supabase.ts             # Supabase 客户端
│   ├── types.ts                # 全局 TypeScript 类型定义
│   ├── constants.ts            # 枚举：分类、地区、人群等
│   └── utils.ts                # 通用工具函数
├── hooks/
│   ├── usePolicies.ts
│   └── useSearch.ts
└── public/
    └── ...
```

---

## 3. 数据库设计

### 3.1 主表：`policies`

```sql
CREATE TABLE policies (
  -- 基础标识
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  slug            TEXT UNIQUE NOT NULL,          -- URL 友好标识符，例如 blaue-karte-2026

  -- 标题
  title_zh        TEXT NOT NULL,                 -- 中文标题
  title_de        TEXT NOT NULL,                 -- 德文原始标题

  -- 来源信息
  publisher       TEXT NOT NULL,                 -- 发布机构
  official_url    TEXT NOT NULL,                 -- 官方原文链接
  published_at    DATE NOT NULL,                 -- 官方发布时间
  effective_at    DATE,                          -- 生效时间（可为空）

  -- 分类信息
  region_level    TEXT NOT NULL CHECK (region_level IN ('联邦', '州', '市', 'Landkreis')),
  region_name     TEXT NOT NULL,                 -- 具体地区，例如 Baden-Württemberg 或 '联邦'
  category        TEXT NOT NULL,                 -- 政策类别（见 3.2）
  target_groups   TEXT[] NOT NULL DEFAULT '{}', -- 适用人群数组（见 3.3）

  -- 核心内容
  summary_zh      TEXT NOT NULL,                 -- 一句话总结
  key_changes     TEXT[] NOT NULL DEFAULT '{}', -- 关键变化（3-5条）
  user_notes      TEXT NOT NULL,                 -- 用户注意事项
  content_zh      TEXT NOT NULL,                 -- 中文整理正文（富文本 HTML）
  content_de_summary TEXT,                       -- 德文原文摘要（可为空）

  -- 状态与质量
  review_status   TEXT NOT NULL DEFAULT 'ai_draft'
                  CHECK (review_status IN ('ai_draft', 'human_reviewed', 'expert_reviewed')),
  risk_level      TEXT NOT NULL DEFAULT 'medium'
                  CHECK (risk_level IN ('low', 'medium', 'high')),
  status          TEXT NOT NULL DEFAULT 'draft'
                  CHECK (status IN ('draft', 'pending_review', 'published', 'unpublished', 'expired')),

  -- 关联（可空，用于标记被替代的旧政策）
  superseded_by   UUID REFERENCES policies(id),

  -- 统计
  view_count      INTEGER NOT NULL DEFAULT 0,

  -- 时间戳
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 全文搜索索引（中文 + 德文）
-- 注意：PostgreSQL 默认不支持中文分词，见第 7 节处理方案
CREATE INDEX policies_fts_idx ON policies
  USING GIN (to_tsvector('german', title_de || ' ' || title_zh || ' ' || summary_zh || ' ' || content_zh));

-- 常用筛选索引
CREATE INDEX policies_status_idx ON policies(status);
CREATE INDEX policies_category_idx ON policies(category);
CREATE INDEX policies_region_name_idx ON policies(region_name);
CREATE INDEX policies_region_level_idx ON policies(region_level);
CREATE INDEX policies_published_at_idx ON policies(published_at DESC);
CREATE INDEX policies_target_groups_idx ON policies USING GIN(target_groups);
```

### 3.2 分类枚举（`category` 字段）

```typescript
// lib/constants.ts
export const CATEGORIES = [
  { value: '居留与签证',   label: '居留与签证',   icon: '📋' },
  { value: '留学与大学',   label: '留学与大学',   icon: '🎓' },
  { value: '工作与蓝卡',   label: '工作与蓝卡',   icon: '💼' },
  { value: '入籍与长期居留', label: '入籍与长期居留', icon: '🏠' },
  { value: '税务与社保',   label: '税务与社保',   icon: '📊' },
  { value: '医保与保险',   label: '医保与保险',   icon: '🏥' },
  { value: '家庭与福利',   label: '家庭与福利',   icon: '👨‍👩‍👧' },
  { value: '交通与驾照',   label: '交通与驾照',   icon: '🚗' },
  { value: '宠物与犬税',   label: '宠物与犬税',   icon: '🐾' },
  { value: '生活行政',     label: '生活行政',     icon: '📬' },
  { value: '其他',         label: '其他',         icon: '📌' },
] as const;
```

### 3.3 适用人群枚举（`target_groups` 字段，多选数组）

```typescript
export const TARGET_GROUPS = [
  { value: '留学生',     label: '留学生' },
  { value: '求职者',     label: '求职者' },
  { value: '工作签人群', label: '工作签人群' },
  { value: '蓝卡人群',   label: '蓝卡人群' },
  { value: '自雇人士',   label: '自雇人士' },
  { value: '华人家庭',   label: '华人家庭' },
  { value: '新移民',     label: '新移民' },
  { value: '车主',       label: '车主' },
  { value: '宠物主人',   label: '宠物主人' },
] as const;
```

### 3.4 地区数据（`region_name` 字段参考值）

```typescript
export const REGIONS = {
  states: [
    'Baden-Württemberg', 'Bayern', 'Berlin', 'Brandenburg',
    'Bremen', 'Hamburg', 'Hessen', 'Mecklenburg-Vorpommern',
    'Niedersachsen', 'Nordrhein-Westfalen', 'Rheinland-Pfalz',
    'Saarland', 'Sachsen', 'Sachsen-Anhalt',
    'Schleswig-Holstein', 'Thüringen',
  ],
  federalLabel: '联邦',  // region_name = '联邦' 代表全国政策
};
```

### 3.5 自动更新 `updated_at`

```sql
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER set_updated_at
  BEFORE UPDATE ON policies
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();
```

### 3.6 Supabase Row Level Security (RLS)

```sql
-- 只有已发布的内容对公众可见
ALTER TABLE policies ENABLE ROW LEVEL SECURITY;

-- 公共读取：只能读 published 状态
CREATE POLICY "Public read published policies"
  ON policies FOR SELECT
  USING (status = 'published');

-- 管理员可以读写全部（通过 Service Role Key）
-- 后台 API 使用 supabaseAdmin（service role），绕过 RLS
```

---

## 4. 路由与页面规划

### 4.1 路由总览

| 路由 | 页面 | 渲染模式 |
|---|---|---|
| `/` | 首页 | SSR（含最新政策，需实时） |
| `/policies` | 政策列表页 | SSR（筛选参数动态） |
| `/policies/[slug]` | 政策详情页 | ISR（revalidate: 3600） |
| `/categories/[category]` | 分类页 | ISR |
| `/regions/[region]` | 地区页 | ISR |
| `/search` | 搜索结果页 | CSR（客户端搜索） |
| `/about` | 关于我们 | SSG（静态内容） |
| `/admin` | 后台首页 | 服务端 + Auth Guard |
| `/admin/policies` | 政策管理列表 | 服务端 + Auth Guard |
| `/admin/policies/new` | 新增政策 | 服务端 + Auth Guard |
| `/admin/policies/[id]` | 编辑政策 | 服务端 + Auth Guard |
| `/admin/login` | 后台登录 | 服务端 |

### 4.2 URL 参数约定

政策列表页（`/policies`）支持以下 Query Params：

| 参数 | 类型 | 示例 | 说明 |
|---|---|---|---|
| `region_level` | string | `州` | 地区层级筛选 |
| `region_name` | string | `Baden-Württemberg` | 具体地区筛选 |
| `category` | string | `居留与签证` | 政策类别筛选 |
| `target_group` | string | `留学生` | 适用人群筛选 |
| `days` | number | `7`, `30`, `90` | 最近 N 天发布 |
| `date_from` | date | `2026-01-01` | 自定义起始日期 |
| `date_to` | date | `2026-03-31` | 自定义截止日期 |
| `sort` | string | `published_at`, `effective_at`, `risk_level` | 排序方式 |
| `page` | number | `1` | 分页 |

搜索页（`/search`）：

| 参数 | 说明 |
|---|---|
| `q` | 搜索关键词（中文或德文） |

---

## 5. 核心组件规划

### 5.1 PolicyCard（政策卡片）

用于首页、列表页展示。

**Props:**

```typescript
interface PolicyCardProps {
  id: string;
  slug: string;
  titleZh: string;
  titleDe: string;
  publisher: string;
  regionLevel: string;
  regionName: string;
  publishedAt: string;
  effectiveAt?: string;
  category: string;
  targetGroups: string[];
  summaryZh: string;
  reviewStatus: 'ai_draft' | 'human_reviewed' | 'expert_reviewed';
  riskLevel: 'low' | 'medium' | 'high';
  status: string;
}
```

**展示规则：**

- 高风险（`risk_level = 'high'`）：卡片左侧显示红色竖条
- 中风险：显示黄色竖条
- 低风险：显示灰色竖条
- `review_status = 'ai_draft'`：显示「AI 初步整理」标签，颜色灰色
- `superseded_by` 不为空：显示「已有新版」提示

### 5.2 FilterPanel（筛选面板）

**行为：**
- 移动端：筛选面板可折叠（默认收起）
- PC端：筛选面板固定在列表左侧（sidebar 形式）
- 每次筛选变更，通过 `router.replace()` 更新 URL params，不触发页面重载

**筛选联动逻辑：**
- 选择 `region_level = '联邦'` 时，`region_name` 筛选自动置灰/隐藏
- `days` 和 `date_from/date_to` 互斥，选一个时自动清除另一个

### 5.3 PolicyDetail（政策详情页主体）

**布局结构（从上到下）：**

```
┌─────────────────────────────────────────────────┐
│ [高风险/过期 警告横幅，条件显示]                  │
├─────────────────────────────────────────────────┤
│ 中文标题                                         │
│ Deutscher Originaltitel（德文小字）              │
│ [类别标签] [地区标签] [风险等级] [复核状态]       │
├─────────────────────────────────────────────────┤
│ 📋 基础信息                                      │
│   发布机构 / 官方链接 / 发布时间 / 生效时间        │
├─────────────────────────────────────────────────┤
│ 💡 一句话总结                                    │
├─────────────────────────────────────────────────┤
│ 👥 这条政策影响谁                                 │
│   [人群标签列表]                                 │
├─────────────────────────────────────────────────┤
│ 🔑 关键变化                                      │
│   • 变化1                                       │
│   • 变化2                                       │
│   • 变化3                                       │
├─────────────────────────────────────────────────┤
│ ⚠️ 你可能需要注意什么                             │
├─────────────────────────────────────────────────┤
│ 📄 中文整理正文（富文本）                         │
├─────────────────────────────────────────────────┤
│ 📎 德文原文摘要（可选，折叠展示）                 │
├─────────────────────────────────────────────────┤
│ 🔗 官方来源（突出展示）                           │
│   [跳转官方原文按钮]                             │
├─────────────────────────────────────────────────┤
│ ⚖️ 免责声明                                      │
├─────────────────────────────────────────────────┤
│ 📌 相关政策推荐                                  │
└─────────────────────────────────────────────────┘
```

**条件显示规则：**

| 条件 | 显示内容 |
|---|---|
| `risk_level = 'high'` | 页面顶部红色 Banner：「该内容仅为官方公开信息的中文整理，不构成个案法律、税务或移民建议。」 |
| `status = 'expired'` 或 `superseded_by != null` | 页面顶部黄色 Banner：「该政策已于 XXX 后被新规定替代，请查看最新版本。」，并附跳转链接 |
| `effective_at` 存在 | 显示生效时间字段 |
| `content_de_summary` 存在 | 显示德文原文摘要折叠区块 |

### 5.4 RiskBadge（风险等级徽章）

```typescript
const RISK_CONFIG = {
  low:    { label: '低风险', color: 'bg-gray-100 text-gray-600' },
  medium: { label: '中风险', color: 'bg-yellow-100 text-yellow-700' },
  high:   { label: '高风险', color: 'bg-red-100 text-red-700' },
};
```

### 5.5 ReviewBadge（复核状态徽章）

```typescript
const REVIEW_CONFIG = {
  ai_draft:        { label: 'AI 初步整理', color: 'bg-gray-100 text-gray-500' },
  human_reviewed:  { label: '人工复核',    color: 'bg-blue-100 text-blue-700' },
  expert_reviewed: { label: '专家复核',    color: 'bg-green-100 text-green-700' },
};
```

---

## 6. API 设计

### 6.1 GET `/api/policies`

获取政策列表，支持筛选、排序和分页。

**Query Params：**

```
region_level, region_name, category, target_group,
days, date_from, date_to,
sort (default: published_at),
page (default: 1), page_size (default: 20)
```

**Response:**

```json
{
  "data": [PolicyCard],
  "total": 128,
  "page": 1,
  "page_size": 20
}
```

**Supabase 查询示例：**

```typescript
let query = supabase
  .from('policies')
  .select('id, slug, title_zh, title_de, publisher, region_level, region_name, published_at, effective_at, category, target_groups, summary_zh, review_status, risk_level')
  .eq('status', 'published');

if (region_level) query = query.eq('region_level', region_level);
if (region_name)  query = query.eq('region_name', region_name);
if (category)     query = query.eq('category', category);
if (target_group) query = query.contains('target_groups', [target_group]);
if (days)         query = query.gte('published_at', subDays(new Date(), days).toISOString());

const sortColumn = sort === 'effective_at' ? 'effective_at'
                 : sort === 'risk_level'   ? 'risk_level'
                 : 'published_at';
query = query.order(sortColumn, { ascending: false });
query = query.range((page - 1) * pageSize, page * pageSize - 1);
```

### 6.2 GET `/api/policies/[id]`

获取单条政策完整内容（包括关联的"替代政策"信息）。

**Response 包含所有 `policies` 表字段**，额外添加：

```json
{
  ...policy,
  "superseded_by_policy": {
    "slug": "blaue-karte-2026-neu",
    "title_zh": "蓝卡薪资门槛调整新规（2026）"
  }
}
```

### 6.3 GET `/api/search`

**Query Params:**
- `q` — 搜索关键词（必填）
- `region_name`, `category`, `days` — 可选筛选

**Response:**

```json
{
  "data": [SearchResult],
  "total": 12,
  "query": "蓝卡"
}
```

`SearchResult` 结构：

```typescript
interface SearchResult {
  id: string;
  slug: string;
  titleZh: string;
  summaryZh: string;
  regionName: string;
  publishedAt: string;
  category: string;
  targetGroups: string[];
  officialUrl: string;
  riskLevel: string;
}
```

### 6.4 Admin API（需 Service Role Key，绕过 RLS）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/admin/policies` | 获取所有状态的政策（含草稿） |
| POST | `/api/admin/policies` | 新建政策 |
| PATCH | `/api/admin/policies/[id]` | 更新政策字段 |
| PATCH | `/api/admin/policies/[id]/status` | 单独更新状态（发布、下架等） |
| DELETE | `/api/admin/policies/[id]` | 删除政策（慎用，建议用下架） |

**Admin API 验证：**

```typescript
// 每个 Admin API Route 在头部执行：
const session = await getSession(request);
if (!session || session.role !== 'admin') {
  return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
}
```

---

## 7. 搜索实现

### 7.1 问题背景

PostgreSQL 内置 FTS 使用 `to_tsvector`，对中文支持非常有限（中文无空格分词）。

### 7.2 第一阶段方案（简单可用）

使用 PostgreSQL `ILIKE` 模糊匹配，覆盖以下字段：

```sql
SELECT * FROM policies
WHERE status = 'published'
AND (
  title_zh ILIKE '%蓝卡%'
  OR title_de ILIKE '%蓝卡%'
  OR summary_zh ILIKE '%蓝卡%'
  OR content_zh ILIKE '%蓝卡%'
  OR publisher ILIKE '%蓝卡%'
)
ORDER BY published_at DESC;
```

**优点：** 零配置，立即可用，中文德文均支持。
**缺点：** 性能低，无相关性排序。第一阶段数据量小时完全够用。

### 7.3 第二阶段升级方案（数据量增大后）

可选以下任一方案升级：

**方案 A：Supabase + `pg_trgm`（三元组模糊匹配）**
```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE INDEX policies_trgm_idx ON policies
  USING GIN ((title_zh || summary_zh || content_zh) gin_trgm_ops);
```
支持中文字符子串匹配，性能优于 ILIKE。

**方案 B：集成 Algolia（推荐长期方案）**
- 在政策发布时 Webhook 触发 Algolia 索引同步
- Algolia 天然支持中文分词搜索和关键词高亮
- 免费 tier：10,000 条记录

---

## 8. 内容状态机

政策内容有以下 5 种状态，转换规则如下：

```
              [运营人员创建]
                    │
                    ▼
                 draft（草稿）
                    │ 提交复核
                    ▼
            pending_review（待复核）
                    │ 审核通过
                    ▼
              published（已发布） ──── 用户可见
                    │
          ┌─────────┴─────────┐
          │ 手动下架           │ 标记过期
          ▼                   ▼
     unpublished（已下架）  expired（已过期）
          │ 可重新发布
          ▼
       published
```

**状态说明：**

| 状态 | 前台可见 | 后台可见 | 说明 |
|---|---|---|---|
| `draft` | ✗ | ✓ | 编辑中，未提交 |
| `pending_review` | ✗ | ✓ | 已提交，等待人工复核 |
| `published` | ✓ | ✓ | 正常展示 |
| `unpublished` | ✗ | ✓ | 主动下架，保留内容 |
| `expired` | ✗（历史存档） | ✓ | 政策已失效 |

**注：** `expired` 状态下，若用户持有旧详情页链接，应显示提示 Banner 而非 404。

---

## 9. 后台管理

### 9.1 登录机制

- 使用 Supabase Auth（Email + Password）
- 仅限管理员账号（手动在 Supabase 控制台创建，不提供注册入口）
- Session 通过 Cookie 持久化
- Admin Layout 中间件验证 Session，未登录跳转 `/admin/login`

### 9.2 政策编辑表单字段

```typescript
// PolicyForm 包含以下受控字段

// 必填字段
titleZh: string        // 中文标题，文本输入
titleDe: string        // 德文原始标题，文本输入
publisher: string      // 发布机构，文本输入
officialUrl: string    // 官方链接，URL 输入 + 预览按钮
publishedAt: Date      // 发布时间，日期选择器

// 可选字段
effectiveAt?: Date     // 生效时间，日期选择器

// 分类字段（下拉选择）
regionLevel: '联邦' | '州' | '市' | 'Landkreis'
regionName: string     // 联动：选联邦后锁定为'联邦'；其他时显示州/市下拉
category: string       // 政策类别，下拉选择
targetGroups: string[] // 适用人群，多选 Checkbox

// 核心内容
summaryZh: string      // 一句话总结，文本输入（限 100 字）
keyChanges: string[]   // 关键变化，动态增删行（3-5条）
userNotes: string      // 用户注意事项，多行文本
contentZh: string      // 中文正文，Tiptap 富文本编辑器
contentDeSummary?: string // 德文摘要，多行文本（可选）

// 质量控制（下拉选择）
reviewStatus: 'ai_draft' | 'human_reviewed' | 'expert_reviewed'
riskLevel: 'low' | 'medium' | 'high'

// 状态（按钮操作）
status: 'draft' | 'pending_review' | 'published' | 'unpublished' | 'expired'

// 关联
supersededBy?: string  // 被替代政策的 ID，可选
```

### 9.3 发布操作 UI

编辑页底部操作区：

```
[保存草稿]   [提交复核]   [发布]   [下架]
```

- 「保存草稿」：保存当前内容，状态设为 `draft`
- 「提交复核」：状态设为 `pending_review`
- 「发布」：直接发布（`published`），快捷操作
- 「下架」：仅在已发布时显示

### 9.4 列表管理页

支持按状态快速筛选（类似 Tab）：

```
全部 | 草稿 | 待复核 | 已发布 | 已下架 | 已过期
```

每行显示：中文标题、分类、地区、风险等级、复核状态、发布时间、操作按钮

---

## 10. 非功能需求实现

### 10.1 移动端适配

- 全站使用 Tailwind 响应式前缀（`sm:`, `md:`, `lg:`）
- 首页政策列表：移动端单列，PC 端双列或三列
- FilterPanel：移动端抽屉式（Drawer），PC 端侧边栏
- PolicyCard：确保在 360px 最小宽度下可正常阅读

### 10.2 性能优化

- 政策详情页使用 ISR（`revalidate: 3600`），平衡实时性与性能
- 政策卡片图片（如有）使用 `next/image` 自动优化
- 首页最新政策列表限制展示 10 条
- 列表页分页：每页 20 条

### 10.3 SEO

- 每个政策详情页动态生成 `<title>` 和 `<meta description>`：

```typescript
// app/(public)/policies/[slug]/page.tsx
export async function generateMetadata({ params }): Promise<Metadata> {
  const policy = await getPolicy(params.slug);
  return {
    title: `${policy.titleZh} | 德区政策展台`,
    description: policy.summaryZh,
    openGraph: {
      title: policy.titleZh,
      description: policy.summaryZh,
    }
  };
}
```

- 政策列表页生成 `robots.txt` 和 `sitemap.xml`（通过 Next.js 内置支持）

### 10.4 免责声明交互

- 每个政策详情页固定底部显示免责声明文字（不可被用户关闭）
- 高风险政策：顶部红色 Banner 额外再次提示
- `about` 页面完整法律声明

---

## 11. 部署方案

### 11.1 环境变量

```env
# .env.local
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx...    # 公共只读（受 RLS 限制）
SUPABASE_SERVICE_ROLE_KEY=eyJxxx...         # 仅服务端使用（Admin API）
```

### 11.2 Vercel 部署步骤

1. 连接 GitHub 仓库到 Vercel
2. 配置上述环境变量
3. 配置 `vercel.json`（可选，如有自定义 Header 需求）

### 11.3 Supabase 初始化

1. 创建 Supabase 项目
2. 执行第 3 节的 SQL 建表语句
3. 配置 RLS Policy
4. 在 Supabase Auth 中手动创建管理员账号

---

## 12. 开发优先级与 Milestone

### Milestone 1 — 基础展示（约 2 周）

**目标：** 能录入政策内容并展示给用户

- [x] 数据库建表 + RLS
- [x] 首页（静态骨架 + 最新 5 条政策）
- [x] 政策详情页（完整字段展示）
- [x] 后台登录 + 新增/编辑政策表单
- [x] 政策卡片组件

### Milestone 2 — 筛选与搜索（约 1 周）

**目标：** 用户能按需找到政策

- [x] 政策列表页 + FilterPanel
- [x] URL 参数驱动的筛选逻辑
- [x] 搜索页（ILIKE 方案）
- [x] 分类页 + 地区页

### Milestone 3 — 内容管理完善（约 1 周）

**目标：** 运营人员可流畅管理内容

- [x] 后台列表管理（状态 Tab 筛选）
- [x] 状态流转按钮（草稿→复核→发布→下架）
- [x] 被替代政策关联字段
- [x] 关于我们页面

### Milestone 4 — 体验打磨（持续）

- [x] 移动端全面适配
- [x] SEO Meta 标签
- [x] Sitemap 生成
- [x] 高风险 Banner 逻辑
- [x] 过期政策提示 Banner
- [x] 相关政策推荐（底部）

---


*文档生成时间：2026-05-03 | 如有需求变更请同步更新本文档*
